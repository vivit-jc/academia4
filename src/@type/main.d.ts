type Command = {
    text:string,
    link:string
}

type Branch = {
    text: string,
    effect: string,
    link: string
}

type Sdata = {
    month: number,
    day: number,
    scenes: string[][]
}